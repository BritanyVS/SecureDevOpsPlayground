{
  "Version": "2017-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "secretsmanager:GetSecretValue",
      "Resource": "arn:aws:secretsmanager:us-east-1:123456789012:secret:securedevops/*",
      "_comment": "EJEMPLO CORRECTO: el valor NO está en el repo. La app llama en runtime: GetSecretValue → DB_PASSWORD"
    }
  ],
  "_example_runtime_usage": {
    "_comment": "En la app se leería así. El secreto real vive en AWS Secrets Manager, no aquí.",
    "step_1": "aws secretsmanager get-secret-value --secret-id securedevops/db",
    "step_2": "Set-Item Env:DB_PASSWORD $secret.Value  # o en un secret ref del contenedor",
    "step_3": "la app lee Environment.GetEnvironmentVariable('DB_PASSWORD')"
  }
}